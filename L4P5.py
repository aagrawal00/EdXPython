def clip(lo, x, hi):
    '''
    Takes in three numbers and returns a value based on the value of x.
    Returns:
     - lo, when x < lo
     - hi, when x > hi
     - x, otherwise
    '''
##    if x==min(lo,x):
##        retvalue=lo
##    elif x==max(x,hi):
##        retvalue=hi
##    else:
##        retvalue=x
##    return retvalue
    return retvalue(min(hi,max(lo,x)))
